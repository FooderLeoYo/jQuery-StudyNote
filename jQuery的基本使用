1. 入口函数：等页面DOM加载完后再执行js
	$(function() {
		想执行的函数
	})
	
2. $是jQuery的别称，也是jQuery的顶级对象

3. jQuery对象与DOM对象相互转换
	3.1 DOM对象装换为jQuery对象
		3.1.1. 直接拿到标签
			$('标签名');
		3.1.2. 已经使用过原生js获取过DOM对象，则括号内不需要加引号
			$(对象名);
	3.2 jQuery对象转换为DOM对象，有两种方式
		3.2.1. $('标签名')[索引号]
		3.2.2. $('标签名')get[index]
