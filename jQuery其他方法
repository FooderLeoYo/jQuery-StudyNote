1. 对象拷贝
	语法：
	$.extend([deep], targe, object1, [object2, ...])
	1. deep:如果设为true为深拷贝，默认false浅拷贝
	2. target:要拷贝到的目标对象
	3. object1:待被拷贝的第一个对象
	4. 会覆盖掉属性名相同的数据
	5. 浅拷贝是把被拷贝对象中的复杂数据类型属性的地址拷贝给目标对象，因此修改目标对象会影响被拷贝对象
	6. 深拷贝则是拷贝内容而不是地址

2. 多库共存
	其他js库也可能会使用$作为标识符，为了避免引起冲突：
	1. 用jQuery替代$
	2. 用户自定义标识符：var 自定义名字 = $.noConflict();

3. jQuery插件
	3.1 插件网站：
		1. http://www.jq22.com
		2. http://www.htmleaf.com
	3.2 插件的引入和使用
		视频：p62
	3.3 图片懒加载
		只加载用户可视区的图片，当滑动可视区域时再加载其他的图片
	3.4 全屏滚动（fullpage.js）
		点击向下箭头，整个屏幕换到下一张图片
	3.5 bootstrap组件和插件
		视频：p65-66
